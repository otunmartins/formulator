# Formulator

Next.js (App Router) + TypeScript + Tailwind CSS.

```bash
npm install
npm run dev     # http://localhost:3000
npm run lint
npm run build
```

Features are built one spec at a time with the `/feature` workflow in Claude Code. Specs are in `context/features/`, and the active feature is tracked in `context/current-feature.md`.

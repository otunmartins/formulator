@AGENTS.md

# Feature workflow

Work is spec-driven through the `/feature` skill (`.claude/skills/feature/SKILL.md`). Specs live in `context/features/`, reference images in `context/screenshots/`. The active feature and its progress are tracked below. Keep that file up to date while working on a feature.

@context/current-feature.md
